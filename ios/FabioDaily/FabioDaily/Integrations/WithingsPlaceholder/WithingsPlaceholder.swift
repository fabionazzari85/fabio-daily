import Foundation

struct WithingsPlaceholder {
    let isActive = false

    var message: String {
        "Withings diretto non è attivo. Prima si leggerà il peso da Apple Health, anche quando arriva da Withings."
    }
}
